# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| v5.12.x | ✅ 受支持（最新） |
| v5.11.x | ⚠️ 仅安全修复 |
| 更早版本 | ❌ 不受支持 |

## 报告漏洞

- **不要公开提交 issue 描述漏洞细节**（会泄露给未修复前的所有用户）。
- 请发邮件到项目维护者，或先建一个**不包含细节**的 issue 占位，再私下沟通细节。
- 收到报告后我们会在 7 天内确认，修复后同步发布补丁版本。

## 已知安全基线

- 渠道 API 密钥只存服务端（DB/env），前端只见掩码；离线 APK 默认剥钥（`OFFLINE_EMBED_KEYS` 未设置时）。
- 渠道 baseUrl 强制 https 且禁内网/回环地址（SSRF 防护，22 变体实测）。
- 密码只存 scrypt 加盐哈希；认证三级守卫（public/private/admin），写库走 private 且 userId 取自 session（防 IDOR）。
