# run: v5.4 全站审览（第七期公告 · 横幅摘要化 · 结构文档）
- 时间: 2026-08-03
- 标准: verifier/v6/CRITERIA.md
- 范围: 管理员实发第七期公告 → 全站八路审览 → 部署模拟 → 结构文档

## 公告发布（admin 实发）
- 「第七期 · 纸上功夫，持续在线」id=30001；DeepSeek 公告风格：四件事技术事实先行 → 诚实说明边界 → 工具论 → 关于迭代 → 《荀子·非十二子》古训收束
- 公告榜截图 v7_notices.png：StepSeal 期号 / 最新一期徽标 / 全文排版符合古风契约 ✓

## 发现并已修复的问题（本轮真实缺陷，全部闭环）
1. **首页横幅全文上墙**：publishNotice 同步全文到 siteSettings.announcement，Home 原样渲染 → 一整墙公告压住首页。修复：Home 只渲染摘要（【标题】+ 正文压空白截 72 字 + 「公告榜 →」链接，整卡可点击）。截图 v54_home_guest/user.png 验证 ✓
2. **游客点横幅落在签到门**：TicketsPage 默认「我的工单」页签对游客只显示签到卡。修复：ready && !user 时默认落「公告榜」。截图 v54_guest_tickets.png ✓
3. **测试套件污染公告榜**：test_v53_api.py 的 T22 故意留一期测试公告，审览期间它压过第七期成为横幅。修复：套件收尾自清（撤掉自建公告），并手动清掉存量污染 id=30002/3/1 ✓
4. **冒烟断言依赖污染数据**：smoke_v53 断言公告榜含「定制卷上线」。修复：改断「最新一期」徽标（内容无关）✓
5. **服务重启中断量规任务**：换横幅重启 prod 时 92 项量规跑到一半被判 error「服务重启，任务中断，可点重试从断点续跑」——生命周期设计按预期工作，重跑至 92/92 ✓

## 八路审览结果（全绿）
| 套件 | 结果 |
|---|---|
| API v5 | 76 通过 / 0 失败 |
| API v5.1 | 35 通过 / 0 失败 |
| API v5.3 | 22 通过 / 0 失败 |
| 量规 exam ref 3（全流水线阅卷） | 92/92 全部达标 |
| 冒烟 v5 | 30/30 |
| 冒烟 v5.1 | 15/0 |
| 冒烟 v5.3（修断言后重跑） | 18/0 |
| 冒烟·使用手册 | 10/0 |

## 部署模拟（干净树）
- git archive HEAD → /tmp/v54_sim → npm install → npm run build → PORT=3100 启动
- home:200 / tickets:200 / siteInfo 返回第七期横幅 / 迁移幂等收敛 / 种子完成 ✓
- .env 不入档 ✓；模拟实例用完即杀（按 /proc cwd 精确识别，主实例 1303 无恙）——防止共享库下僵尸清扫误伤主实例任务

## 结构文档
- README.md 重写：项目结构图 + 加新模块六步标准动作 + 设计红线（只加不改/officialOf 判分基准/密钥服务端/语料不分发/任务生命周期/古风契约）
- 手册同步：功能地图 12 项（含工单）+ FAQ 8 条（反馈印报障、公告在哪看）

## 增补：用户实测反馈三修复（同日第二轮）
1. **窄屏双导航**：顶栏 14 项全量导航在 <768px 时与底部 TabBar 并存挤压。修复：顶栏导航 `hidden md:flex`，窄屏由底部 TabBar + 更多抽屉独占导航，顶部仅留品牌/字号/模型/签到。截图 v541_mobile_home.png ✓
2. **横幅直达公告榜**：登录用户点横幅仍落「我的工单」。修复：横幅链接带 `?tab=notices`，TicketsPage 读查询参数直达公告页，页签切换同步 URL。截图 v541_notices_direct.png（登录态直达）✓
3. **公告简介（digest）**：announcements 加 `digest TEXT NULL`（迁移 0005，TiDB TEXT 不可带默认值）；发布表单新增「一句话简介」选填，留空服务端自动取正文首段截 90 字；横幅同步格式改为 `【标题】简介`，撤下回退同样用简介；公告榜正文上方以朱砂左边线引文样式展示简介；管理端历期列表显示简介。第七期已回填简介并刷新横幅 ✓
- 回归：test_v53_api 22/0（含发布/撤下/横幅回退）、smoke_v53 18/0、smoke_v5 30/30、smoke_v51 15/0、smoke_manual 10/0
- 插曲：重启时 3000 端口被 vite dev server 抢占（EADDRINUSE），digest 迁移改为手动执行补齐；vite dev 读源码即时生效，功能验证无碍；dist 构建产物与快照不受影响
