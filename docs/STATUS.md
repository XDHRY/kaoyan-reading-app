# 项目现状与下一步（STATUS）

> 更新日期：2026-09-18。本文回答三个问题：现在做到哪了、已知缺陷是什么、接下来做什么。
> 功能与使用见 [使用手册.md](使用手册.md) / [功能导览.md](功能导览.md)；开发约定见 [开发指南.md](开发指南.md)。

## 现状一句话

v5.12.5（2026-08-16 发布）功能完整、处于维护期：真题库 / 六阶段解析 / 错题闭环 / 生词本 / 作文工坊 / AI 生题全部可用，三端（Web / Windows EXE / Android APK）交付物在 Releases 长期提供。

## 工程健康

- **CI 全绿**（2026-09-17 升级）：Node 22、离线数据库构建、Chromium 真实浏览器冒烟、Android 云端构建。
- **门禁**：`npm run check`（tsc）+ `npm run lint`（eslint）+ `npm run build` 三绿。
- **测试**：`verifier/v1/` 边界套件 137 项 + 核心 API 76 项 + 补充 40 项可随时全量跑（不触发真实 LLM）；Vitest 单元测试已起步（首批覆盖 API 限速器，且「0 tests」在 CI 计为红灯）。
- **安全**：npm audit 告警从 28 项降到 **7 项**（非破坏性批次，2026-09-17）；第三方依赖已建立可缓存 vendor 边界。

## 已知缺陷 / 技术债

| 事项 | 严重度 | 说明 |
|---|---|---|
| npm audit 剩余 7 项 | 中 | 跟踪于 issue #2，需分批评估（部分涉及构建链主版本升级，不能无脑 `audit fix --force`） |
| 单元测试覆盖薄 | 中 | Vitest 目前只覆盖限速器；核心 router / 判分链路仍依赖 Python 边界套件间接覆盖 |
| Windows 桌面版依赖本机 MySQL 8 | 中 | 普通用户需先装数据库（或设 `KYSOP_MYSQL_BIN`），是桌面版最大上手门槛；APK 与 Web 自部署无此问题 |
| 无 iOS 客户端 | 低 | iPhone/iPad 只能走 Web 自部署；Capacitor iOS 工程未排期 |
| 无英语二内置真题 | 低 |  workaround 已写入 FAQ（用 AI 生题模拟）；版权口径决定不能随意收编 |

## 下一步（建议优先级）

1. **分批清理剩余 7 项 npm audit**（issue #2）：逐项评估破坏性，构建链相关项单独开 PR 验证。
2. **扩展 Vitest 覆盖**：优先判分 `officialOf` 链路、认证三级守卫、渠道中台路由选择——这些是「改坏了用户立刻有感」的地方。
3. **桌面版数据库体验**：评估免装方案（内置 MySQL 便携化 / SQLite 桌面料），消除 Windows 版最大门槛。
4. **v5.13 规划**：从工单反馈与错题 AI 诊断的使用数据中提炼下一版功能点。
